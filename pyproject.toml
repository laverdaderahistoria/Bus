[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bus"
version = "0.1.0"
dependencies = ["requests"]

[tool.vercel]
entrypoint = "api.bus:handler"
